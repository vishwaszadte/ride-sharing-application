<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ride-Sharing Application</title>
    <style>
      /* Style the body */
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
      }

      /* Style the driver detail container */
      .driverDetailContainer {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      /* Style the driver detail card */
      .driverDetailCard {
        width: 400px;
        padding: 20px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        text-align: center;
      }

      /* Style the driver detail card title */
      .driverDetailCard h1 {
        font-size: 24px;
        margin-bottom: 10px;
      }

      /* Style the driver detail card subtitle */
      .driverDetailCard h2 {
        font-size: 20px;
        margin-bottom: 10px;
      }

      /* Style the driver detail card subtext */
      .driverDetailCard span {
        font-weight: normal;
      }

      /* Style the driver detail card estimated cost */
      .driverDetailCard h3 {
        font-size: 18px;
        margin-bottom: 20px;
      }

      /* Style the driver detail card button */
      .driverDetailCard button {
        background-color: #4caf50;
        color: #fff;
        padding: 10px 20px;
        font-size: 16px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.2s ease-in-out;
      }

      /* Style the driver detail card button on hover */
      .driverDetailCard button:hover {
        background-color: #3e8e41;
      }
    </style>
  </head>
  <body>
    <div class="driverDetailContainer">
      <% if (driver) { %>
      <div class="driverDetailCard">
        <h1><b>Name: </b> <%= driver.name %></h1>
        <h2>
          <b>Vehicle: </b> <%= driver.vehicleName %>
          <span> (<%=driver.vehicleType %>) </span>
        </h2>
        <h3><b>Estimated cost: </b> 150</h3>
        <form action="">
          <button>Start negotiation</button>
        </form>
      </div>
      <% } %> <% if (error) { %>
      <div class="errorContainer">
        <h2>error</h2>
      </div>
      <% } %>
    </div>
  </body>
</html>
